import {Fragment} from "react";
import {NavLink, Outlet} from "react-router-dom";

const Ranking = (props) => {
    return (
        <Fragment>
            <section>

            </section>
        </Fragment>
    )
};
export default Ranking;