import {Fragment} from "react";
import {NavLink, Outlet} from "react-router-dom";

const Details = (props) => {
    return (
        <Fragment>
            <section>

            </section>
        </Fragment>
    )
};
export default Details;